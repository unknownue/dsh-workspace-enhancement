# UAT 反馈模板（FEEDBACK）

> 复制本模板，为每条反馈开一个条目（`F-<轮次>-<序号>`），填完贴到对应轮次的 UAT 脚本判定表，
> 并把结论回写到 `docs/rounds/R*.md` 的**遗留**段与 `drafts/CONTEXT.md` §7。
> 严重度 `blocker` **阻塞发布**：必须修复 + 重跑同一份脚本 + 重新签字。
> 流程见 [README.md](./README.md)。

---

## F-`<轮次>`-`<序号>`：`<一句话标题>`

### 1. 环境指纹

| 项 | 值 |
|---|---|
| 实例 / 端口 | `<lab 50599 / 真实 3080>` |
| 插件版本 | `<例 0.1.2>` |
| commit | `<短 sha>` |
| 宿主 OS / 版本 | `<例 Windows + 版本>` |
| 远端（如涉及） | `<OS / 发行版；用占位主机名>` |
| 浏览器 / 视口 / 缩放 | `<浏览器 + 版本>` / `<宽 × 高>` / `<100% / 125%>` |
| 主题 | `<浅色 / 深色 / 跟随系统>` |
| 语言 | `<中文 / English>` |
| 相关机器 id | `<c1 / c2 …>` |

### 2. 现象

`<一段话：发生了什么，界面/输出上看到了什么>`

### 3. 复现步骤

1. `<操作>`
2. `<操作>`
3. `<操作>`

**复现率**：`<必现 / 偶现 n/m / 仅一次>`

### 4. 期望

`<期望的行为或界面表现>`

### 5. 实际

`<实际行为，尽量附原文：错误消息、DOM 片段、RPC 响应、日志行>`

### 6. 严重度

- ☐ `blocker` — 阻塞发布（功能不可用 / 数据或安全问题 / 无法完成验收）
- ☐ `major` — 功能可用但明显不正确，需在本轮修复
- ☐ `minor` — 观感 / 文案 / 边界瑕疵，可排后续轮

### 7. 证据

| 项 | 链接 / 内容 |
|---|---|
| 截图 | `$LAB_HOME\e2e\<file>.png` |
| trace / 日志 | `$LAB_HOME\e2e\<file>.log` / `<results.json>` |
| 控制台 / RPC 响应 | `<粘贴关键行>` |
| lab 是否复现 | ☐ 是 ☐ 否（若否，写明真实实例上的复现条件） |

### 8. 是否阻塞发布

- ☐ 是（阻塞：修复 → 重跑 `docs/uat/R*.md` → 重新签字）
- ☐ 否（登记到 `docs/rounds/R*.md` 遗留 + `drafts/CONTEXT.md` §7，排入后续轮）

### 9. 处置（由 agent / 维护者回填）

| 项 | 值 |
|---|---|
| 定性 | `<缺陷 / 已知边界 / 非插件问题（上游/环境）/ 未复现>` |
| 根因 | `<一句话；可留空待查>` |
| 修复提交 | `<短 sha>` |
| 复验结论 | `<重跑脚本通过 / 仍不通过>` |

---

来源：`docs/uat/README.md`；`AGENTS.md` §4/§8；`drafts/CONTEXT.md` §7
