# UAT 脚本 — R6 运行时国际化（三面双语）

> 本文件是 [TEMPLATE.md](./TEMPLATE.md) 的**真实填写示例**，内容取自 R6（I18N）轮的 lab E2E 记录
> （`.agent-teams/archive/dsw-i18n-r6/inbox/captain.jsonl` 的 t11 报告 + `CHANGELOG.md` 0.1.2 +
> `drafts/CONTEXT.md` §7）。验收时按本表逐项执行并勾选。
> 流程与 lab 用法见 [README.md](./README.md)；轮次事实见 [../rounds/R6-i18n.md](../rounds/R6-i18n.md)。

## 0. 基本信息

| 项 | 值 |
|---|---|
| 轮次 / 主题 | R6（I18N）— 运行时国际化：客户端 UI / 远程认知提示 / `sw_*` 工具面三面双语 |
| 验收对象 | 设置页 Language 行切换后，三面文案与布局是否即时、持久、无回归 |
| 需求 / 缺陷 ID | CONTEXT §5.12（R6 I18N）；附带两处设置页布局回归（用户截图报告） |
| 脚本作者 / 日期 | 验证角色（verifier）/ 2026-09-01 |
| 预期耗时 | 25–40 分钟（含两次重启） |

## 1. 前置条件

| 项 | 值 |
|---|---|
| 版本 | `0.1.2`（`CHANGELOG.md` 0.1.2 节，日期 2026-08-31；**未发布**） |
| 分支 | 来源未记录 R6 分支名（提交为仅本地、未 push）——验收时按现场填写 |
| commit | `754b933`（本地 7 commit 链末端：依赖修复 `52e9242` → 词典/管道 `e15c1de` → 客户端迁移 `5380dd0` → 宿主迁移 `184c4a0` → 文档 `0a43158` → `3573f14` 0.1.2 prepare → `754b933` 门禁脚本） |
| 构建产物 | `lib/` 已重建（host + `lib/client.js`）；`npm run build` 通过 |
| lab 地址 | `http://127.0.0.1:50599/` |
| 启动命令 | `pwsh -File scripts/dev-lab.ps1`（重启用 `-SkipBuild`） |
| 浏览器与视口 | Chromium（browser-use 0.13.8 驱动）/ **视口来源未记录**——验收时按实际填写 |
| 主题 | 跟随系统（lab 偏好 = `system`） |
| 语言 | 起始 `中文`，步骤中切到 `English` 再切回 |
| 缩放 | `<现场填写>` |
| 其他前置 | 已保存机器 `c1`（`user@192.0.2.10:22`，key 认证，工作目录 `/home/<user>`）；lab 与真实实例组合同构 |

**禁止**：在 3080（真实实例）上执行本脚本；修改产品 profile；使用真实凭据。

## 2. 步骤

| # | 操作 | 期望 | 观察点 | 结果 |
|---|---|---|---|---|
| 1 | `pwsh -File scripts/dev-lab.ps1` 启动 lab，打开 `http://127.0.0.1:50599/` | 实例就绪，插件已注入 | HTTP 200；页面引用 `<pkg>/client.js`；`POST /dsw/connections.list` → `ok:true` | ☐ 通过 ☐ 不通过 |
| 2 | 打开设置页，找到 **Language** 行，中文 → **English** | 客户端 UI 即时切换（无需刷新） | 设置页 section 标题 `远程工作区` → `Remote workspaces` | ☐ 通过 ☐ 不通过 |
| 3 | 打开「添加工作区」流程 | 流程文案随语言 | `选择工作区目录` → `Choose a workspace directory` | ☐ 通过 ☐ 不通过 |
| 4 | 查看机器表单（设置页 + flow 两处） | 表单字段/按钮文案双语一致 | 按钮文案成对：`保存/清空/测试连接` ↔ `Save/Clear/Test connection`；`保存并浏览` ↔ `Save and browse` | ☐ 通过 ☐ 不通过 |
| 5 | 查看会话栏行徽标（`row-badges`） | 语言切换**就地重绘**，不重建 DOM、不闪烁 | **8 枚**徽标 `connected` → `已连接` 即时更新；徽标节点未变 | ☐ 通过 ☐ 不通过 |
| 6 | 检查语言偏好持久化文件 | preference 双向写入 | `settings.yaml` 的 `locale.preference`：`en` → 切回后 `zh` | ☐ 通过 ☐ 不通过 |
| 7 | 新建远程会话：flow 选 `c1` → 浏览 `/home/<user>` → 「连接并打开」 | 会话创建成功，宿主面随语言组装 | 出现新会话 + 轨迹 Turn 1；框架运行时记录 `System Prompt and Tools Updated`（切换后回合生效） | ☐ 通过 ☐ 不通过 |
| 8 | 在该会话触发 `sw_*` 工具（如 `sw_status`） | 工具面按当前语言组装 | 工具描述 / 参数说明 / 输出 / 错误消息均为当前语言 | ☐ 通过 ☐ 不通过 |
| 9 | 切回中文，重启 lab 实例（`dev-lab.ps1 -SkipBuild`） | 偏好持久 | 重启后 UI 直接中文、Language 行 = 中文、flow 文案为中文（**3/3**） | ☐ 通过 ☐ 不通过 |
| 10 | 布局回归 A：设置页「已配置的机器」行，分别在 `en` / `zh` 下查看 | 不破板、按钮不重叠 | 信息列可换行、按钮组右对齐或整体换行；任意宽度下不互相挤压 | ☐ 通过 ☐ 不通过 |
| 11 | 布局回归 B：设置页「添加机器」的 Port / Username 行 | 不溢出卡片 | Port 行止于约 491 px、Username 行止于约 675 px，均在卡片约 690 px 内（滚动截图取证） | ☐ 通过 ☐ 不通过 |
| 12 | 例外核对：机器可解析标记与协议层诊断 | 两语逐字节一致 / 保留原文 | `[exit code: N]`、`[stderr]`、`[timed out after Nms]` 两语相同；`bad-request:` 保留英文；品牌与枚举值不变 | ☐ 通过 ☐ 不通过 |

## 3. 判定

| 项 | 值 |
|---|---|
| 结论 | ☐ 通过 ☐ 不通过 |
| 通过项 / 总项 | `<n>/12`（R6 轮自动化记录为 4/4 验收组 + 9+5 项检查全过） |
| 证据链接 | `$LAB_HOME\e2e\`：`i18n_t11_pha.py` / `i18n_t11_phb2.py` / `i18n_t11_phc.py`、14 张截图、`results.json`、运行日志、轨迹转储；布局回归：`i18n_layout_probe.py`、`i18n-layout-en.png`、`i18n-layout-zh-after.png`、`i18n-layout-portuser-scroll.png` |
| 未通过项 | `<指向 FEEDBACK 条目>` |
| 是否阻塞发布 | ☐ 是 ☐ 否 |
| 用户签字 | `<姓名 / 日期>` |

## 4. 环境指纹

| 项 | 值 |
|---|---|
| 实例 / 端口 | lab / 50599（隔离 `DSH_HOME`） |
| 插件版本 | `0.1.2` |
| commit | `754b933` |
| 宿主 OS / 版本 | Windows（版本 `<现场填写>`） |
| 远端（如涉及） | Linux（`c1`，文档中以占位主机名表示） |
| 浏览器 / 视口 / 缩放 | Chromium（browser-use 0.13.8）/ `<现场填写>` / `<现场填写>` |
| 主题 / 语言 | `system`（浅色 + 深色两态）/ 中文 ↔ English |
| 相关机器 id | `c1` |

## 5. 备注

- **诚实限制（来源已披露）**：轨迹面板的 `Initial System Prompt` 为虚拟化折叠渲染，headless 下
  6 种展开/提取路径均取不到正文（会话转写存储仅骨架 216 字符），属展示层限制、非插件缺陷——
  建议在真实界面**人工点击确认**像素级内容。
- **未复现项**：用户截图顶部的「空白边框条」在 lab 相同宽度下未复现（探针 `empties=[]`，两轮）；
  若 3080 换装 0.1.2 后仍见该空条，需单独定位（可能为 shell 层残留 / 加载态）并按 FEEDBACK 回填。
- 验收全程不得触碰 3080 与真实 `%DSH_HOME%`；结束后停掉 50599 并清理 lab 状态。
- 例外边界：仅 `i18n-design.md` §13-1~8 既定清单（机器可 parse 标记 / `tool call aborted` /
  `bad-request:` 协议层 / 品牌与枚举值 / 库原始错误 / 注释日志 / 数量词 / 动态键禁止）。

来源：`.agent-teams/archive/dsw-i18n-r6/inbox/captain.jsonl`（t11 报告、t21 终评）；`CHANGELOG.md` 0.1.2；`drafts/CONTEXT.md` §7；`scripts/dev-lab.ps1`
