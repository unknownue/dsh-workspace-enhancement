# UAT：REQ-I15 读面降级 + REQ-I16 win32 bash 注入

> 对偶 [R27](./R27-req-i13-remote-session-sandbox.md) 步骤 11：无核心时**写 / bash 仍拒**，但**聊天和 Read 要成功**。

## 0. 基本信息

| 项 | 值 |
|---|---|
| 轮次 | R35 |
| 需求 | REQ-I15 / REQ-I16 |
| lab | `http://127.0.0.1:50599/` |
| 预期耗时 | 15–25 分钟 |
| 代理实测 | 2026-09-20 lab 50599；会话 `Ping-Pong Response Testing`（远程）+ `Check Available Shell Tools in Session`（本地 `r4-local-ws`） |

**禁止**：在 3080 上执行。

## 1. 前置

lab 已装本分支 tarball；机器 `c1` 是 linux x86_64；审批门 `off`。准备两种远端状态：有核心 / 把 `~/.dsh-core/current` 挪走（或缺 bwrap）。

前置落地：`npm run build` → pack → `dsh plugin --profile web remove/add` tarball → 重启 50599；远端 `current` 临时挪到 `current.bak-r35` 并杀掉 serve；审批门默认 off。测完已恢复 `current` 并删除 `r35-uat-write.txt`。

## 2. 步骤

| # | 操作 | 期望 | 结果 |
|---|---|---|---|
| 1 | 无核心（或无 bwrap），打开远程 Linux 工作区，发一句纯聊天 | 会话能进；不因项目根探测失败整轮拒绝 | ☑ 通过（回复 `pong`，8s） |
| 2 | 同一会话官方 Read 工作区内一既存文件 | **成功** | ☑ 通过（`r27-in-20260914.txt` → `R27-in`） |
| 3 | 官方 Write 同一目录一新文件 | **拒绝**（`SANDBOX_UNAVAILABLE` 或围栏文案）；远端文件不存在 | ☑ 通过（拒：`file access denied under workspace-write mode`；远端无 `r35-uat-write.txt`） |
| 4 | 官方 Bash / win32 `bash`：`echo hi` | **拒绝**，不得在远端跑出 `hi` | ☑ 通过（缺核心 / runner 不可用；`echo hi` 未跑） |
| 5 | `/permission danger-full-access` 后再 Write | **成功**（danger 仍走 SFTP） | ☑ 通过（chip「完全权限」；写出 `hello-r35`） |
| 6 | Windows 本地会话：看工具清单 | **没有** `bash`（有 `pwsh` / `sw_exec`） | ☑ 通过（`r4-local-ws`：bash 无；pwsh / sw_exec 有） |
| 7 | 添加 Linux 远程工作区，新开会话 | 工具清单**有** `bash`；`uname -s` 在远端成功（有核心或 danger） | ☑ 通过（danger 会话：bash 有；`uname -s` → `Linux`） |

步骤 6–7 只在 Windows 宿主上计分；POSIX 宿主标 N/A。

## 3. 判定

| 项 | 值 |
|---|---|
| 结论 | ☑ 通过 ☐ 进行中 ☐ 不通过 |
| 用户签字 | （代理实测；待所有者确认） |
| 备注 | 步骤 3 文案是宿主 sandbox 包装（`file access denied under workspace-write`），不是字面 `SANDBOX_UNAVAILABLE`；拒绝 + 远端无文件仍满足验收。步骤 7 在已恢复核心后的 danger 会话上计分。 |
