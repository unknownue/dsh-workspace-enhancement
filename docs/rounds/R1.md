# R1 — 抛光轮：5 项已知边界 + 更名 + 清理

## 目标

关闭 R0.6 遗留的 5 项已知边界（`browse.home` 错误透传、keychain→明文 UI、错误消息脱敏、
`test()` 接 keychain、`nextId` 竞态），并完成**更名与迁移**（RPC 渠道 `/dsh-ssh`→`/dsw`、
错误前缀 `dsh-ssh:`→`dsw:`、占位目录新根 `dsw-routes` + 旧 `dsh-ssh-routes` 兼容），
顺手清理与文档更新。需求 ID：R1 抛光轮（CONTEXT §6.2 / ROADMAP 已完成轮次表）。

## 拆解

AgentTeams 轮次 `polish-and-ui`（角色：engineer / tester / reviewer）。

| 任务 | 角色 | 依赖 | 内容 |
|---|---|---|---|
| t1 | engineer | — | 五边界修复（浏览错误透传 / keychain 切明文 / 消息脱敏 / test 接 keychain / nextId 竞态） |
| t2 | engineer | t1 | 更名：`/dsh-ssh`→`/dsw`、前缀 `dsh-ssh:`→`dsw:`、占位目录兼容 |
| t3 | engineer | t2 | 清理遗留（镜像 meta 目录）+ README/docs 更新 |
| t4 | tester | t3 | 沙箱 E2E 回归（五边界 + 更名全链） |
| t5 | reviewer | t4 | 终审（边界修复 / 更名 / 清理） |
| t6 | engineer | — | 修 W1（picker 惰性连接）+ 缺陷2（test 空私钥路径）+ 旧 `.secrets` 清理 |
| t7 | tester | — | 复验：干净启动 + 三态 test + `.secrets` 清理 + 回归 |
| t8 | engineer | t6 | 小修：私钥读取失败脱敏（含展开路径）+ runtime `secretValues` 补全 + README 占位符 |
| t9 | reviewer | t8 | 最终微审（t8 三修闭环） |

## 验证

- **单测**：51/51（t9 独立复跑，0 fail）；typecheck 0 错误；build 成功且 `lib/` 随源码重建。
- **E2E（t4 → t7）**：首轮 34 项检查 29 PASS / 5 FAIL，暴露 **W1 阻断级问题**——R1 产物在 lab 无法启动
  （`fatal load failure: All configured authentication methods failed`），根因是 picker 构造器内
  急切 `remoteHome()` 触发占位 `ssh-remote` 行的认证；t6 改为**惰性连接**（挂载期零连接）后，
  t7 复验 13/13 全 PASS：干净启动（无 overlay）HTTP 200、`browse.home` 错误透传
  （错误密码机器返回 `ok=false` 且不泄密码、不返回假 home）、`machines.test` 三态（keychain /
  错误密码 / key 机器均真实认证，不再 `ENOENT open ''`）。
- **评审**：t5 **needs-fix**（小修级：2 处脱敏缺口 + 1 处文档占位符，其余全部 approve）→ t8 修
  （`~` 展开路径与 readFileSync 绝对路径同时 `<redacted>`、`resolvePrivateKey` 失败消息重写、
  runtime `secretValues` 补目标机私钥）→ t9 **APPROVE**。
- **真机验证**：真实实例重启后全绿——`/dsw` 通道可用、旧渠道 405、`status` `connected=false`
  （零连接启动语义）、TOFU 记录完好。

## 结果

- 更名完成：`/dsw` 唯一一对同步（host `rpc.handle('/dsw')` + client `rpc.call('/dsw')`）、
  `dsh-ssh:` 前缀 0 残留、`sshRoutesRoot` → `dsw-routes` 且 `routeFromPlaceholder` 兼容旧
  `dsh-ssh-routes` 树（存量会话 cwd 不失效，3 条回归覆盖）。
- 五边界全部关闭；镜像 meta 目录已删；README（EN/ZH）与 docs 同步。
- 上线：已部署 3080（ROADMAP R1 行：✅ 上线）。

## 遗留

- writer 提出待确认项：3080 是否已用**修复后**构建重启（t1 验证的是修复前构建）。
- `terminal.ts` 的 `cd` 无 `--`（继承上游原样）未处理。
- 旧 `dsh-ssh-routes/` 占位树保留兼容读取，人工清理归 **A5**。
- 后续轮引用：R2（UI 统一）、R3（A1 抛光）、R6 I18N（文案键化的前置面）。

来源：`.agent-teams/archive/polish-and-ui/team.json` + `inbox/captain.jsonl`；`drafts/CONTEXT.md` §6.2、`docs/ROADMAP.md`
