# R3 — A1 抛光轮

## 目标

关闭工程遗留 A1：机器表单交互守卫与缓存一致性四项（P2-① inflight 去重粒度、
P2-② 跳板不合法静默丢弃、P2-③ 双击双发无 busy 守卫、P2-④ 编辑 key 机丢私钥认证）+
F2 编辑保存后表单内提示瞬失 + **C3 同名误标修复**（本地工作区与远程同名时被误标为远程）。
需求 ID：A1（`drafts/ideas.md` 工程遗留表 + 2026-08-23 拍板）。

## 拆解

AgentTeams 轮次 `r3-polish`（角色：engineer / tester / reviewer）。

| 任务 | 角色 | 依赖 | 内容 |
|---|---|---|---|
| t1 | engineer | — | A1 抛光：P2 四项 + F2 + C3 同名误标修复 |
| t2 | tester | t1 | A1 E2E：六项修复验证 + 回归 |
| t3 | reviewer | t2 | A1 终审（六项修复） |
| t4 | engineer | — | 补 C3 撤回：重建时清除不再符合条件的行徽标 |
| t5 | tester | — | t4 撤回补验（同名后出现 → 徽标清除） |
| t6 | engineer | — | 修 t4 回归：徽标自持循环（`rowTitleOf` 排除徽标文本） |
| t7 | tester | — | t6 重验：撤回四项 + churn=0 断言 |
| t8 | engineer | — | A1 附加：跳板链可清除（编辑态）+ ttl 死参清理 |
| t9 | tester | — | t8 快验：跳板清除（编辑态）+ 新建不变 + 回归 |

## 验证

- **单测**：100/100（CONTEXT §6.2 记录；t3 评审时 97/97，t6/t8 新增用例后为 100）；
  typecheck 0 错误。
- **E2E（t2）**：26/26 全 PASS——双击双发（同 tick 连点「测试连接」/「保存」各只发 1 次 RPC、
  机器只创建 1 条）；坏跳板 `user@` → 血色 `role=alert` 且测试/保存 0 次 RPC，合法链放行恰 1 次；
  编辑 key 机留空私钥仍保留 `privateKeyPath` 且原机私钥可用；C3 同名（本地↔远程、远程↔远程）无徽标、
  唯一 `.zcode` 有 🌐 已连接、`.ssh` 行始终有徽标（负对照）；F2 页面级提示在、表单内无 success；
  回归（连接并打开 → 工作区+会话落盘、设置页 CRUD）通过。
- **t4 → t6 回归闭环**：t5 补验发现 **t4 真实回归**——徽标 withdraw/re-inject 无限循环
  （零交互 8s churn 76/76，闪烁）；t6 修（`rowTitleOf` 排除徽标文本）后 t7 重验 12/12，
  **churn add/rem = 0/0**，徽标节点未变（无闪烁副作用）。
- **t8 快验（t9）**：9/9 全 PASS——编辑态清空跳板链全链路证明（存储跳板确实参与连接 →
  清空后直连成功 → 落盘 `jump: []`）。
- **评审**：t3 **APPROVE**（六项 + t4 撤回 + t6 回归全部达标，73 基线零退化）。

## 结果

- 交付：`InflightKey = ${endpoint}:${id}` 键控、`jumpErrorOf` 提交前硬校验、`createActionGate`
  同步 busy 守卫、编辑态认证保留（`machinePayload`）、F2 页面级提示、C3 同名判定用
  `ctx.workspaces` 全量集合、徽标撤回与零写收敛、编辑态 `jump:[]` 契约、ttl 死参清理。
- 上线：已部署 3080（运行态 `client.js` 173 KB + `conn.status` 零连接）。

## 遗留

- 行徽标仍是 DOM 增辉层（A2 上游 PR 已撤销，正式槽位注入不可用）。
- 「跳板可清除」仅在编辑态验证（新建态不变，已回归覆盖）。
- 后续轮引用：R4（远程认知与执行）、R6 I18N（徽标文案键化与就地重绘）。

来源：`.agent-teams/archive/r3-polish/team.json` + `inbox/captain.jsonl`；`drafts/CONTEXT.md` §6.2、`drafts/ideas.md` A1
