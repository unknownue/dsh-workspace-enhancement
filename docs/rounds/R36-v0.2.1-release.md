# R36 — 发布 npm `v0.2.1`

- **日期**：2026-09-20
- **需求**：`PUB-6`（随本轮进 master 的：`REQ-I10` / `REQ-I15` / `REQ-I16`、`BUG-7` / `BUG-8` / `BUG-9`）
- **范围**：版本 bump + 核心工件 `0.2.0 → 0.2.1`；不含 UPSTREAM-5 / REQ-I14

## 1. 目标

把已进 `master` 的 0.2.0 之后改动发布为 `0.2.1`：日落 `sw_pick_workspace`、围栏档读面降级、
win32 bash 按工作区注入，以及远端任务停止 / CAS version / 宿主 tar 三个修复。

## 2. 拆解

1. 关账：`REQ-I15` / `REQ-I16` 从 §1 挪到 §4（代码与 lab UAT 已随 PR #28 合入）。
2. 版本：`package.json` / lock、`CHANGELOG` 0.2.1 段、`core/artifact.json` + 生成的
   `src/core-artifact.ts`、Go `coreArtifactVersion`（0.2.0 发布时 hello 仍写着 `0.2.0-dev`，
   本轮对齐到 `0.2.1`）。
3. 兼容窗口：`docs/compatibility.md` §1 把当前发布改到 0.2.1；宿主仍是 `^0.1.5-rc.1`。
4. tag `v0.2.1` → `release.yml` 排队 → 所有者在 `npm-publish` 点 Approve → OIDC 发布。

## 3. 验证

产品代码已在 #26 / #27 / #28 的 CI 全绿。本轮是版本与文档；静态闸门要求 `package.json`、
CHANGELOG、`docs/status.md` 同为 `0.2.1`。核心 hello 与 `artifact.json` 同为 `0.2.1`。

真机：I15/I16 的 lab UAT 见 [`../uat/R35-req-i15-i16.md`](../uat/R35-req-i15-i16.md)；
BUG-9 见 [`../uat/R32-bug9-stop-uat.md`](../uat/R32-bug9-stop-uat.md)。

## 4. 结果

- 准备提交；npm 发布等所有者推 tag 并在 Actions 点 Approve。
- 宿主窗口不变：`^0.1.5-rc.1`。
- **对外行为**：旧会话再调 `sw_pick_workspace` 会未知工具；已部署 0.2.0 核心的机器需要再
  `core.deploy` 一次才能拿到组杀与对齐后的 `version`。
- backlog：`REQ-I10` / `REQ-I15` / `REQ-I16` / `BUG-7` / `BUG-8` / `BUG-9` → done；`PUB-6` shipped。

## 5. 遗留

- 远端四组合 UAT（有/无 bwrap × 有/无 rg）仍未跑（0.2.0 起的尾巴）。
- `UPSTREAM-5` 仍等 `0.1.6-rc`；alpha 红灯继续挂着。
- 3080 仍不装本插件。

来源：本轮发布准备。
