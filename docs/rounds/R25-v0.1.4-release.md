# R25 — 发布 npm `v0.1.4`

- **日期**：2026-09-13
- **需求**：`PUB-4`
- **范围**：版本 bump + tag；不含 `REQ-I5` 核心（预定 0.2.0）

## 1. 目标

把已进 `master` 的 0.1.5 家族支持、副根权限退役、远程审批门、会话级机器连接与远端 spawn 围栏发布为 `0.1.4`。

## 2. 拆解

版本号 / Changelog 日期 / 兼容窗口 / `PUB-4` 行 → CI → squash 进 `master` → tag `v0.1.4` → `release.yml` 排队 → 所有者在 Actions 的 `npm-publish` 点 Approve。

## 3. 验证

产品代码已在 #16 的 CI 全绿（ubuntu 22/24 + windows 22）。本轮只改版本与文档；静态闸门要求 `package.json`、CHANGELOG、`docs/status.md` 同为 `0.1.4`。

## 4. 结果

- npm 包 `dsh-workspace-enhancement@0.1.4` 已发布（OIDC Trusted Publishing；所有者在 `npm-publish` 点 Approve 后上架）
- 宿主窗口：`^0.1.5-rc.1`；与 0.1.3 不兼容（通道路径、`sw_connect` 参数面）
- `PUB-4` → `shipped`（2026-09-15）

## 5. 遗留

- R24 两份真机 UAT 仍待所有者在 lab 50599 跑
- 围栏只覆盖 spawn 的口径已随 `REQ-I5` 收口（核心 RPC）；实现在 `feat/REQ-I5-remote-core`，预定 0.2.0
- 3080 仍不装本插件；换装由所有者决定
