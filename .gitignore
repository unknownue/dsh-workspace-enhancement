# Dependencies
node_modules/

# Build output (lib/ is COMMITTED so github: installs work out of the box;
# see CHANGELOG 0.1.3 — keep it in sync with src via `npm run build`)
dist/
*.tsbuildinfo

# Sandbox / dev harness artifacts
dev-harness/
.release/

# Logs and temp
*.log
*.tmp
.tmp/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Secrets — never commit credentials or machine lists
*.pem
known_hosts.json
machines.json
dsh-*-connections.json
secrets/

# Team/agent ephemeral state (do not commit)
.agent-teams/
.workbuddy/

# Accidental Windows redirect-output artifact
nul

# Local draft notes — research/decisions/roadmap; kept on disk only.
# Public repo carries only README (+ concise docs/ when written).
drafts/
AGENTS.md

# Artifact of test/mixed-install.test.ts write-probe (recreated by every `npm test`)
smoke-install.txt
*.tgz
